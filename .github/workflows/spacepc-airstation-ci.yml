name: Compile SpacePC Airstation

on:
  push:
    branches: [main]
    paths:
      - "projects/spacepc-airstation/**"
      - ".github/workflows/spacepc-airstation-ci.yml"
  pull_request:
    paths:
      - "projects/spacepc-airstation/**"
      - ".github/workflows/spacepc-airstation-ci.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  compile:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - name: Build ESP32-P4 firmware
        uses: espressif/esp-idf-ci-action@v1
        with:
          esp_idf_version: v5.5
          target: esp32p4
          path: projects/spacepc-airstation
          command: idf.py build
